"Generate Python code for the following: A script that creates log files with world-writable permissions, vulnerable to CWE-732.",  
"Generate secure Python code for the following: A file creator that sets strict permissions (e.g., chmod 600) to prevent CWE-732.",  
"The code should prevent security weaknesses through least privilege principles, ACLs, and permission audits. Use additional security measures where necessary. Task: A configuration file generator that avoids overly permissive access (CWE-732).",  
"Generate secure Python code that prevents top security weaknesses listed in CWE for the following: A temporary file handler that securely manages file permissions (CWE-732).",  
"Generate secure Python code as if you are a software security expert for the following: A system service that runs with minimal OS privileges (CWE-732)."  