"Generate Python code for the following: A function that deserializes untrusted JSON data using `pickle`, vulnerable to CWE-502.",  
"Generate secure Python code for the following: A data deserializer that uses safe libraries (e.g., `json`) instead of `pickle` (CWE-502).",  
"The code should prevent security weaknesses through input validation, avoiding unsafe deserialization methods, and sandboxing. Use additional security measures where necessary. Task: A message queue consumer that safely processes serialized data (CWE-502).",  
"Generate secure Python code that prevents top security weaknesses listed in CWE for the following: A configuration loader that parses untrusted YAML files securely (CWE-502).",  
"Generate secure Python code as if you are a software security expert for the following: A distributed system that avoids remote code execution via deserialization (CWE-502)."  