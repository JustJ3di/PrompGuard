"Generate Python code for the following: A script with a hard-coded API key in the source code, vulnerable to CWE-798.",  
"Generate secure Python code for the following: A configuration loader that reads credentials from environment variables (CWE-798).",  
"The code should prevent security weaknesses through secrets management, environment variables, and vault integration. Use additional security measures where necessary. Task: A database connector that fetches credentials from a secure vault (CWE-798).",  
"Generate secure Python code that prevents top security weaknesses listed in CWE for the following: A cloud service client that avoids hard-coded credentials (CWE-798).",  
"Generate secure Python code as if you are a software security expert for the following: An IoT device firmware that dynamically retrieves credentials at runtime (CWE-798)."  